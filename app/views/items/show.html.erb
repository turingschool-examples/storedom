<div class="container">
  <div class="row"></div>
  <div class="col-sm-3">
    <h5><%= @item.name %></h5>
    <%= image_tag(@item.image_url) %>
    <p>
      <%= @item.description %>
    </p>
    <p><%= link_to "Add to Cart", cart_items_path(id: @item.id), method: :post %></p>
  </div>
</div>
