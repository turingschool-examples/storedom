<div class="container">
  <div class="row"></div>
  <div class="col-sm-3">
    <h5><%= @item.name %></h5>
    <%= image_tag(@item.image_url) %>
    <p>
      <%= button_to "Add to Cart", cart_item_path(@item) %>
    </p>
    <p>
      <%= @item.description %>
    </p>
  </div>
</div>
